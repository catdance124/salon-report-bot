[project]
name = "salon-report-bot"
version = "0.1.0"
description = "サロンレポートPDFをNotebookLMで分析しLINE Worksに送信するbot"
requires-python = ">=3.12"
dependencies = [
    "notebooklm-py[browser]",
    "google-api-python-client",
    "google-auth",
    "pdfplumber",
    "python-dotenv",
    "PyJWT",
    "requests",
]
